# Lists of students and their test scores (parallel lists)
students = ["Alice", "Bob", "Charlie", "Diana"]
test_scores = [88, 92, 79, 95]

# Print each student's score
for i in range(len(students)):
    print(f"{students[i]} scored {test_scores[i]} points.")
